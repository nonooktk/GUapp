# GU EC サイト P1 ST 実施記録（異常・境界・セキュリティ）

| 項目 | 内容 |
| --- | --- |
| 実施日 | 2026-09-24 |
| 実施者 | クロミ代行（QA 悲観視点） |
| 対象 | テスト設計書 GUEC-TD-01 draft-v2: 3.1 区分「異常」「境界」12 件（ST-F001-02, F007-01, F009-03/04/05, F013-02, F014-02/03/04/06, F025-02/03）、3.1 区分「正常（DB 変更要）」2 件（ST-F032-01, 02）、3.2 セキュリティ 8 件（ST-SEC-02, 03, 04, 05, 06, 08, 10, 11） |
| 環境 | Mac・Docker MySQL 8.4（コンテナ `guapp-mysql`）・Next.js dev（`pnpm dev`、`http://localhost:3000`、ログ `/tmp/guapp-web.log`）・FastAPI `http://127.0.0.1:8000`（`uv run uvicorn app.main:app --port 8000`、ログ `/tmp/guapp-api.log`） |
| ブラウザ | Claude Code 内蔵ブラウザ（Chromium）。API は curl（BFF 経由 `localhost:3000/api/...` と FastAPI 直 `127.0.0.1:8000/api/v1/...` の両方）を併用 |
| 独立性 | 正常系の実施者（ペックル／シナモロール代行）とはすり合わせをしていない |

ルール: 期待結果の文言をそのまま観測できたときだけ「合格」。観測できなかったもの・環境の都合で実施できないものは「未実施（理由）」。推測で合格にしない。

作成した注文番号（本記録の実施中に確定した全 8 件。すべて DB SELECT で確認済み）:

| 注文番号 | 用途 | ステータス | 合計 |
| --- | --- | --- | --- |
| GU-260924-4LUTUF8M | ST-F025-02・ST-SEC-08 の照会対象（ST-F009-03/04 で組んだカートをそのまま確定） | accepted | ¥159,500 |
| GU-260924-3XZP7DPF | ST-SEC-05（配送先氏名・住所に SQL/スクリプト注入文字列） | accepted | ¥2,540 |
| GU-260924-JT3CSYQR | ST-F014-02（V-STOCK1 を先に購入した側） | accepted | ¥3,040 |
| GU-260924-4D2X37X3 | ST-F014-04（`PAYMENT_STUB_RESULT=ng`） | payment_failed | ¥2,540 |
| GU-260924-KQW3ETNX | ST-F014-06 試行 1（権限方式。結果的にメール送信は成功） | accepted | ¥2,540 |
| GU-260924-XY98ZQ84 | ST-F014-06 試行 2（権限方式。結果的にメール送信は成功） | accepted | ¥2,540 |
| GU-260924-9WHAGSB8 | ST-F009-05 のため V-STOCK1 とは別の変体（オックスフォードシャツ ブルー/L、id 78）を完売させた側 | accepted | ¥14,950 |
| GU-260924-JR88VD4T | ST-F014-06 再試行（`.mail-outbox` をファイルに置換。メール送信失敗を再現） | accepted | ¥2,540 |

## 1. システムテスト 3.1 機能要件（区分「異常」「境界」）

| ID | 手順（実施したこと） | 期待結果 | 観測結果 | 判定 | 証拠 |
| --- | --- | --- | --- | --- | --- |
| ST-F001-02 | FastAPI 直 `GET /api/v1/products?per_page=24` で非公開商品（P-UNPUB, id 30）の有無を確認。`GET /api/v1/products/30` 直打ち。BFF 一覧 `GET /api/products` も同様に確認。ブラウザで `http://localhost:3000/products/30` を開く | 一覧に出ない。URL 直打ちの商品詳細は 404 | 一覧: `total=30`（公開 30／非公開 1 除外）、id 30 は FastAPI 直・BFF 経由とも 0 件。詳細: FastAPI 直 `GET /api/v1/products/30` → `404 {"code":"not_found"}`。ブラウザ: `/products/30` を開くと見出し「ページが見つかりません」「URL が間違っているか、商品が取り扱い終了になった可能性があります。」を表示、ネットワークログで `GET http://localhost:3000/products/30 → 404 Not Found` を確認 | 合格 | curl 応答（上記）、ブラウザ `read_page`／`read_network_requests` |
| ST-F007-01 | V-STOCK0（`GU-029-BK-M`、variant id 170）を FastAPI 直 `POST /api/v1/cart/items` で追加。ブラウザで `/products/29` を開き ブラック／M を選択 | ボタン無効。API 直叩きでも 409 `out_of_stock` | curl: `409 {"code":"out_of_stock","items":[170]}`。ブラウザ: ブラック／M を選ぶと通常表示の「カートに入れる」ボタンと下部固定バーの両方が `disabled=true`（`querySelectorAll('button')` で 2 件とも `disabled:true, text:"在庫切れ"`）、`aria-live` は「在庫切れ」表示（色・サイズボタンにも「（在庫切れ）」の注記） | 合格 | curl 応答、ブラウザ JS 実行結果（`button.disabled`） |
| ST-F009-03 | 新規カートに variant 68（ドライポロシャツ ネイビー/M、在庫潤沢）を追加後、FastAPI 直 `PATCH /api/v1/cart/items/{id}` で数量を 10 → 11 に変更。ブラウザでも `/products/12` で同じ組み合わせを数量 10 で追加後、続けて数量 10 のまま再度「カートに入れる」を押す（線 10+10=20 で同じ上限に抵触） | 10 は成功、11 は 422 でエラー表示 | curl: qty=10 → `200`（`quantity:10`）。qty=11 → `422 {"code":"limit_exceeded","field":"quantity","limit":10}`。直後の `GET /api/cart` で `quantity` は 10 のまま（ロールバック確認）。ブラウザ: qty10 追加でカートバッジ 1→11 点。続けて追加 → トースト「1 つの商品は 10 点までです」が表示され、カートバッジは 11 点のまま変化なし | 合格（注記: カート画面の数量セレクトは `MAX_QUANTITY_PER_ITEM=10` が上限のため、UI から直接「11」を選ぶ操作自体は存在しない。商品詳細ページの追加操作で線数量が上限を超える組み合わせを作ることで同じ 422 経路を再現した → 総括 4.3） | curl 応答、`components/CartView.tsx` の `select` 選択肢生成（`Array.from({length: MAX_QUANTITY_PER_ITEM}, ...)`）、ブラウザ `find` によるトースト文言確認 |
| ST-F009-04 | 同じカートに variant 74・75・76・182 を各 qty10 で追加し合計 50 点にした後、FastAPI 直 `POST /api/v1/cart/items` で variant 183 を qty1 追加。ブラウザでも同様に 5 明細×10 点＝50 点まで積み上げ、6 明細目を追加 | 50 は成功、51 は 422 | curl: 5 明細追加後 `GET /api/cart` の `item_count=50`。6 明細目 → `422 {"code":"limit_exceeded","field":"quantity","limit":50}`、直後の確認で `item_count=50` のまま・該当 variant は未追加。ブラウザ: 50 点まで到達後、追加操作で トースト「カートに入れられるのは合計 50 点までです」が表示され、カートバッジは 50 点のまま変化なし | 合格 | curl 応答、ブラウザ `find` によるトースト文言確認とバッジ表示 |
| ST-F009-05 | ①（V-STOCK1 経由）ST-F014-02 の実施後、負けた側のカート（variant 158 を含む）を `GET /api/v1/cart` で確認。②（ブラウザ経由）ブラウザのカートに variant 78（オックスフォードシャツ ブルー/L、在庫 5）を qty1 追加した状態で、別カート（curl）が同じ variant を qty5（残り全部）購入して在庫を 0 にし、ブラウザで `/cart` を再読み込み | 該当明細に警告が出て「レジへ進む」が無効 | ①: `GET /api/v1/cart` の該当明細が `"stock_status":"out_of_stock"`、`"can_checkout":false`。②: ブラウザの `/cart` ページで該当明細（オックスフォードシャツ ブルー/L）の直下に `role="alert"` で「⚠ 在庫切れのため購入できません」、カート全体にも「⚠ 在庫切れ・在庫不足の商品があるためレジへ進めません」。「レジへ進む」は `<button disabled aria-disabled="true">`（`<a>` リンクではない）で、JS で `disabled:true` を確認 | 合格 | ①: `GET /api/v1/cart` 応答（本表 ST-F014-02 行）。②: ブラウザ `get_page_text` と `javascript_tool` による `button.disabled` 確認（在庫を使い切った注文: GU-260924-9WHAGSB8） |
| ST-F013-02 | ①FastAPI 直 `POST /api/v1/orders` に `ship_name=""`・`guest_email="invalid-email"` を送信。別リクエストで `ship_postal_code="100001"`（6 桁）を送信。②ブラウザで `/checkout` フォームの氏名を空・郵便番号を `100001`・メールを `invalid-email-format` にして「確認画面へ」を押す | 400 `validation_error` の項目が画面で赤字表示され、確認画面へ進めない | ①curl: `400 {"code":"validation_error","fields":[{"name":"ship_name","reason":"out_of_range"},{"name":"guest_email","reason":"format"}]}`。郵便番号 6 桁のみ不正: `400 {"code":"validation_error","fields":[{"name":"ship_postal_code","reason":"format"}]}`。②ブラウザ: 送信後も URL は `/checkout` のまま（確認画面へ遷移せず）、フィールド直下に「入力してください」（氏名）・「郵便番号は 7 桁の数字で入力してください」・「メールアドレスの形式が正しくありません」を表示。3 件とも `getComputedStyle(...).color === "rgb(196, 35, 43)"`（`--color-danger`）で赤字を確認。ネットワークログにこの送信に対応する `POST /api/orders`／`POST /api/checkout/prepare` は発生せず（クライアント側バリデーションで止めている） | 合格（注記: この 3 項目はクライアント側バリデーション `validateCheckoutForm` が先に検知しサーバーへ送信しない実装。サーバー側 400 `validation_error` も①で別途直接確認済みで、期待結果の「400 で赤字表示・進めない」という利用者から見た挙動そのものは一致 → 総括 4.3） | ①curl 応答。②ブラウザ `get_page_text`・`javascript_tool`（`getComputedStyle`）・`read_network_requests` |
| ST-F014-02 | V-STOCK1（`GU-027-NV-M`、variant 158、在庫 1）を 2 つの独立した curl カート（トークン A・B）に qty1 ずつ追加 → 両方で `POST /checkout/prepare` → B を先に `POST /orders` で確定 → A を（B確定前と同じ display 金額のまま）確定 | 注文は成立せずカート画面に戻り、該当明細に在庫切れ表示。在庫は減らない | B: `201` `GU-260924-JT3CSYQR` `accepted`。B 確定直後の `GET /products/27` で variant 158 が `in_stock:false`（他 5 色/サイズは true）。A: `409 {"code":"out_of_stock","items":[158]}`。A 側 `GET /api/v1/cart` は明細 1 件・`stock_status:"out_of_stock"`・`can_checkout:false`・カートは active のまま。DB: `variants.id=158.stock=0`（A の失敗試行後も 0 のまま＝二重減算なし）。`orders` テーブルは B の 1 行のみでこの時点の総数は 3 件（A の分の pending 行は残らない＝ROLLBACK 済み） | 合格 | curl 応答一式、`SELECT stock FROM variants WHERE id=158`、`SELECT order_number,status FROM orders` |
| ST-F014-03 | カート（variant 68 qty1）で `POST /checkout/prepare` → 表示金額（小計1990/送料550/合計2540）取得 → DB で `products.id=12.price_incl_tax` を 1990→2500 に UPDATE（管理側の価格変更を模擬）→ 古い表示金額のまま `POST /orders` で確定 | 409 で注文手続きに戻り、金額が再表示される。注文は作られない | `409 {"code":"price_changed","amounts":{"subtotal":2500,"shipping_fee":550,"total":3050}}`（再計算後の新価格）。直後の `GET /api/v1/cart` は `subtotal:2500, total:3050`（新価格を反映）で明細 1 件のまま。`orders` の総件数は変化なし（試行前後とも当時 3 件）。BFF 側の分岐（`apps/web/lib/order-errors.ts` 71 行 `case "price_changed"`）は SCR-005（注文手続き）へ戻し `prepare` を呼び直す実装であることをソースで確認 | 合格 | curl 応答、DB SELECT（変更前後）、`lib/order-errors.ts` 参照 |
| ST-F014-04 | API を `PAYMENT_STUB_RESULT=ng` で再起動 → カート（variant 68 qty1）を確定 | 注文手続きの支払い方法へ戻る。DB：orders 決済失敗、payments に ng、在庫は元に戻り、カートは active に戻る | `409 {"code":"payment_failed","order_number":"GU-260924-4D2X37X3"}`。DB: `orders.status=payment_failed total=2540`、`payments.provider=stub result=ng amount=2540`。`GET /api/v1/cart` は明細 1 件（active に復帰）。`variants.id=68.stock` はこの注文の前後で不変（一時引当後に同一トランザクション外で復元される実装をコードで確認済み、`services/orders.py` の `_confirm` 6-ng 分岐）。試験後 API を上書き無しで再起動し `PAYMENT_STUB_RESULT` を既定（未設定＝ok）に戻したことを確認 | 合格 | curl 応答、DB SELECT（orders・payments・variants）、API 再起動ログ |
| ST-F014-06（試行 1: 権限方式） | `apps/api/.mail-outbox` を書き込み不可（`chmod 500` および `chmod 000`）にしてから注文確定を試行。3 通りの手順（① chmod 後に少し間を置いて確定、② chmod 直後に確定、③ chmod 000 のまま操作を挟まず放置して権限自体の維持を確認）で実施 | 注文は成立し完了画面が出る。監査ログにメール失敗が残る | ①②とも意図に反して**メール送信自体が成功**し（`.mail-outbox/GU-260924-KQW3ETNX.txt`・`GU-260924-XY98ZQ84.txt` が作成された）、`audit_logs` に `mail.failed` は記録されなかった（`order.confirm` のみ）。③で原因を切り分けたところ、`chmod 000` 直後は `ls -ld` が `d---------`（権限なし）を示すが、**シェルコマンドを一切挟まず 2 秒待つだけで `drwx------@` に自動的に戻る**ことを確認（3 回連続で再現）。権限を能動的に書き戻しているプロセスがあり、Bash からの `chmod` では書き込み不可状態を維持できない | 未実施（環境要因。詳細は試行 2 の結果とあわせて判定） | `ls -ld apps/api/.mail-outbox` の時系列出力（`d---------` → 2 秒後 `drwx------@`）、`audit_logs` SELECT（`mail.failed` 0 件）、outbox 内ファイル一覧 |
| ST-F014-06（試行 2 再試行: 書き出し先をファイルに置換。コーディネーターのレビュー差し戻し 1 回目） | 権限方式は iCloud 由来と思われる自動復元で失敗と判断し、別手法に変更。手順: ①`mv apps/api/.mail-outbox /private/tmp/guapp-mail-outbox-bak`（中身 6 件を退避）→`touch apps/api/.mail-outbox`（同名の空の通常ファイルを作成）→`ls -ld`で通常ファイルであることを確認。②カート（variant 68 qty1、小計1990/送料550/合計2540）を作成し `POST /orders` で確定。③観測後、`rm apps/api/.mail-outbox && mv /private/tmp/guapp-mail-outbox-bak apps/api/.mail-outbox` で復元し、中身の件数を確認 | 注文は成立し完了画面が出る。監査ログにメール失敗が残る | ①`.mail-outbox` は `-rw-r--r--@ ... 0 ... .mail-outbox`（`file` コマンドで `empty`＝通常ファイル）であることを確認。②`POST /orders` は `201`（`order_number:"GU-260924-JR88VD4T", status:"accepted"`）で正常応答。応答本文にメール関連のエラー情報は一切含まれない。DB: `orders.status=accepted total=2540`。`audit_logs` は `order.confirm` に加え **`mail.failed {"error": "FileExistsError"}` が記録**されている（`OutboxMailAdapter.send_order_confirmation` の `self._dir.mkdir(parents=True, exist_ok=True)` が、同名パスに既に通常ファイルが存在するため `FileExistsError` を送出し、`services/orders.py` の `except Exception` で捕捉→監査ログ記録→`await session.commit()` という設計どおりの経路を実データで確認）。`/tmp/guapp-api.log` には `WARNING app.services.orders: 確認メール送信に失敗 order=GU-260924-JR88VD4T to=t***@example.com (FileExistsError)` の 1 行のみが出力され、スタックトレースや例外詳細はログにも本文にも露出しない。③復元後 `ls -ld apps/api/.mail-outbox` は `drwxr-xr-x@ ... .mail-outbox`（ディレクトリに復帰）、中身は退避前と同じ 6 件（`GU-260924-3XZP7DPF.txt`・`4LUTUF8M.txt`・`9WHAGSB8.txt`・`JT3CSYQR.txt`・`KQW3ETNX.txt`・`XY98ZQ84.txt`。今回のテストで書けなかった `GU-260924-JR88VD4T.txt` は存在しない＝件数不一致なし） | 合格（試行 1 の権限方式は環境要因で不成立だったが、試行 2 の「書き出し先をファイルに置換」で意図どおりメール送信失敗を再現し、期待結果（注文成立・監査ログにメール失敗）を実観測で確認できた） | curl 応答（`POST /orders` 201 本文）、DB SELECT（`orders`・`audit_logs`）、`/tmp/guapp-api.log` の該当 WARNING 行、`ls -ld`／`ls`（退避前後の件数一致） |
| ST-F025-02 | 正当な注文番号（`GU-260924-4LUTUF8M`、`test@example.com`）に対し FastAPI 直 `POST /orders/{no}/lookup` を誤ったメール（`wrong@example.com`）で照会。存在しない注文番号 `GU-260924-ZZZZZZZZ` を正しい形式のメールで照会した場合と応答形状を比較 | 404（存在自体を隠す） | 誤メール: `404 {"code":"not_found"}`。正メール: `200`（本文省略）。存在しない注文番号: `404 {"code":"not_found"}`（誤メールの場合と完全に同じ形）。両者を区別する情報が応答に含まれない | 合格 | curl 応答（3 パターン） |
| ST-F025-03 | API を上書き無しで再起動（プロセス内メモリのレート制限カウンタを 0 にリセット）した直後、FastAPI 直 `POST /orders/GU-000000-AAAAAAAA/lookup` を同一 IP（`127.0.0.1`。`X-Forwarded-For` は付けず接続元 IP で計上＝DS-API-023 の既定経路）で 101 回連続送信 | 100 回目は応答、101 回目は 429 | 100 回目: `404 {"code":"not_found"}`（正常に処理された＝制限内）。101 回目: `429 {"code":"rate_limited"}`、応答ヘッダ `Retry-After: 3599`（約 1 時間の残り窓） | 合格 | curl ループの応答コード、101 回目のレスポンスヘッダ |
| ST-F032-01 | DB で `system_settings.tax_rate` を `"0.10"`→`"0.08"` に UPDATE（変更前後を SELECT で記録）→ カート `POST /checkout/prepare` で再計算 → ブラウザでも大型カート（合計 ¥179,500）の確認画面を再表示 | 「うち消費税」が新税率で再計算される。商品価格は変わらない（税込マスタ） | curl: 変更前 `total=2540, tax_included=230`（=2540×10÷110 切り捨て）。変更後（API 再起動なし）`total=2540, tax_included=188`（=2540×8÷108=188.14→188）。`GET /settings/public` も `tax_rate:"0.08"` を即時反映。ブラウザ確認画面: 合計 ¥179,500 のまま「うち消費税 ¥13,296」（=179500×8÷108=13296.29→13296、実際の表示値と一致）。商品単価（`price_incl_tax`）はいずれの検証でも不変。実施後 `tax_rate` を `"0.10"` に戻し SELECT で確認 | 合格 | curl 応答（変更前後）、ブラウザ確認画面テキスト、DB SELECT（変更前・変更後・復元後） |
| ST-F032-02 | DB で `shipping_fee` を 550→700、`free_shipping_threshold` を 4990→1500 に UPDATE（変更前後を SELECT で記録）→ 閾値をまたぐ 2 つのカートで `POST /checkout/prepare` | 変更後の値で送料が計算される。再デプロイ不要 | 変更前: 小計 1990（閾値 4990 未満）→送料 550。変更後（API 再起動なし）: 同じ小計 1990 は新閾値 1500 **以上**になったため送料 0。別カート（小計 990、新閾値 1500 未満）は新しい送料額 700 を適用（旧設定なら 550 だった）。`GET /settings/public` も新値を即時反映。実施後 `shipping_fee=550`・`free_shipping_threshold=4990` に戻し SELECT で確認 | 合格 | curl 応答（変更前後 2 カート）、DB SELECT（変更前・変更後・復元後） |

## 2. システムテスト 3.2 非機能・セキュリティ

| ID | 手順 | 期待結果 | 観測結果 | 判定 | 証拠 |
| --- | --- | --- | --- | --- | --- |
| ST-SEC-02 | API を `APP_ENV=production` で再起動し `/docs`・`/redoc`・`/openapi.json` を FastAPI 直で確認 | すべて 404 | 3 パスとも `404`。起動自体は正常（`/api/v1/health` は `200 {"status":"ok","db":"ok"}` のまま）。実施後 API を上書き無しで再起動し `/docs` が `200`（開発モードで復帰）に戻ることを確認 | 合格 | curl 応答（3 パス）、再起動後の `/docs` 確認 |
| ST-SEC-03 | 許可外オリジン（`http://evil.example.com`）から FastAPI へ CORS プリフライト（`OPTIONS`）。許可オリジン（`http://localhost:3000`）からの同じプリフライトと比較 | 許可オリジン以外は拒否。`Access-Control-Allow-Origin: *` が出ない | 許可外: `400 Disallowed CORS origin`、応答ヘッダに `access-control-allow-origin` 自体が無い。許可オリジン: `200`、`access-control-allow-origin: http://localhost:3000`（オリジンをそのまま返す。ワイルドカードなし） | 合格 | curl `-i` の応答ヘッダ（両パターン） |
| ST-SEC-04 | カート（variant 68 qty1、正しい表示金額 1990/550/2540）の確認画面相当のリクエストで、`display` を `{subtotal:1, shipping_fee:0, total:1}` に改ざんして `POST /orders` | 409（金額変更）。注文は作られない | `409 {"code":"price_changed","amounts":{"subtotal":1990,"shipping_fee":550,"total":2540}}`（サーバー再計算値を返し改ざん値は無視）。直後の `GET /api/v1/cart` は明細 1 件のまま（注文化されていない） | 合格 | curl 応答、`GET /api/v1/cart` 応答 |
| ST-SEC-05 | 配送先氏名に `' OR 1=1 -- <script>alert(1)</script>`、住所に `<img src=x onerror=alert(1)>東京都千代田区` を入力して `POST /orders` | 文字列として保存・表示され、SQL エラーやスクリプト実行が起きない | `201` で正常に注文成立（`GU-260924-3XZP7DPF`）。応答 JSON の `ship_name`／`ship_address` に入力値がそのまま（エスケープや実行なし）。DB `orders` テーブルを SELECT すると同じ文字列がそのまま保存されている。`SHOW TABLES` は前後で 13 表のまま（不正な DDL 実行なし）、この時点の `orders` 件数も想定どおり 2 件（テーブル破壊や余分な行なし） | 合格 | curl 応答、DB SELECT（`orders.ship_name`／`ship_address`）、`SHOW TABLES` |
| ST-SEC-06 | BFF `POST http://localhost:3000/api/orders` を CSRF Cookie／ヘッダ無しで送信。`POST /api/checkout/prepare` も同様。比較として、先に `GET /api/cart` で `csrf_token` Cookie を取得し、`X-CSRF-Token` ヘッダに載せて再送 | CSRF トークン無しで POST /api/orders → 403 | トークン無し: `POST /api/orders` → `403 {"code":"forbidden"}`。`POST /api/checkout/prepare` → 同じく `403 {"code":"forbidden"}`。正しい CSRF トークンを付けた場合は 403 にならず本来の業務判定（このテスト用の空カートのため `409 empty_cart`）まで進む＝CSRF 検証が確実にゲートしていることを確認 | 合格 | curl 応答（Cookie 無し／有りの 2 パターン） |
| ST-SEC-08 | 実在する注文番号（`GU-260924-4LUTUF8M`）を他人のメールアドレス（`wrong@example.com`）で照会 | メール不一致なら 404 | `404 {"code":"not_found"}`。存在しない注文番号を照会した場合と応答が完全に同一形状（ST-F025-02 参照）で、番号の実在可否をレスポンスから判定できない＝IDOR 防止を確認 | 合格 | curl 応答（ST-F025-02 と同一） |
| ST-SEC-10 | FastAPI 直 `GET /api/v1/cart` を `X-Internal-Token` 無し、および誤ったトークン値で呼ぶ。死活監視 `/api/v1/health` は同条件で確認 | 401 | トークン無し: `401 {"code":"unauthorized"}`。誤トークン: `401 {"code":"unauthorized"}`。`/api/v1/health` はトークン無しでも `200 {"status":"ok","db":"ok"}`（設計どおり死活監視のみ対象外） | 合格 | curl 応答（3 パターン） |
| ST-SEC-11 | `docker compose stop mysql` で DB を停止し、FastAPI 直 `GET /api/v1/cart` と BFF `GET /api/cart` を呼ぶ。API ログでサーバー側に実際の例外が出ているかも確認。`docker compose start mysql` で復旧 | 本文は固定文言。スタックトレース・SQL・接続文字列が出ない | FastAPI 直: `500 {"code":"internal_error","message":"処理中にエラーが発生しました"}`。BFF: 同じ本文・ステータスで透過。応答本文にスタックトレース・SQL文・接続文字列は一切含まれない。API ログ（サーバー側のみ）には `sqlalchemy.exc.OperationalError ... Can't connect to MySQL server on '127.0.0.1' ([Errno 61] Connection refused)` の全文スタックトレースが出力されており、詳細はログのみに留め本文には出さない設計を確認。`/api/v1/health` は同条件で `503 {"code":"db_unavailable"}`（専用の分かりやすいエラーで、こちらも詳細非開示）。MySQL 再開後、`/api/v1/health` が `{"status":"ok","db":"ok"}` に戻るまで約 4 秒（2 回のポーリング）で復帰 | 合格 | curl 応答（FastAPI 直・BFF・health）、`/tmp/guapp-api.log` の該当スタックトレース、復旧後の health 応答 |

## 3. 総括

### 3.1 件数

| 判定 | 件数 | ID |
| --- | --- | --- |
| 合格 | 22 | ST-F001-02, F007-01, F009-03, F009-04, F009-05, F013-02, F014-02, F014-03, F014-04, **F014-06（試行 2 で合格）**, F025-02, F025-03, F032-01, F032-02, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-08, SEC-10, SEC-11 |
| 不合格 | 0 | 該当なし |
| 未実施 | 0 | 該当なし（ST-F014-06 は試行 1 が未実施だったが、試行 2 で合格） |

合計 22 件（3.1 異常・境界 12、3.1 正常＋DB 変更 2、3.2 セキュリティ 8）。ST-F014-06 は 1 回目のレビューで差し戻しを受け、別手法（`.mail-outbox` をファイルに置換）で再試行し合格に至った（差し戻し回数: 1）。

### 3.2 不合格の一覧（再現手順つき）

該当なし（本記録の範囲では不合格 0 件）。

### 3.3 未実施の理由

該当なし（0 件）。ST-F014-06 は試行 1（権限方式）が未実施だったが、試行 2（書き出し先をファイルに置換）で合格した。参考までに試行 1 の記録を残す。

| ID（参考・最終判定は合格） | 理由 |
| --- | --- |
| ST-F014-06 試行 1 | `apps/api/.mail-outbox` を `chmod 500`／`chmod 000` で書き込み不可にしても、シェルコマンドを何も挟まない状態で 2 秒以内に権限が `drwx------` へ自動的に復元されてしまい、メール送信失敗を意図的に発生させられなかった（3 回試行、全て同じ結果）。原因はこのタスクの範囲では未特定だが、リポジトリが `~/Desktop` 配下で iCloud Drive 同期対象になり得る場所にあることと関係している可能性がある（`toolmaker/CLAUDE.md` に同種の同期起因トラブルの既知事例あり）。結果として、この試行で作られた 2 件の注文（GU-260924-KQW3ETNX・GU-260924-XY98ZQ84）はいずれもメール送信が成功しており、`audit_logs` に `mail.failed` は記録されていない。→ コーディネーターのレビューで差し戻しを受け、試行 2（`.mail-outbox` をファイルに置換する方式。iCloud の権限復元の影響を受けない）で再試行し合格 |

### 3.4 テストデータ・テスト設計への気づき（バグではない）

| # | 重大度 | 内容 |
| --- | --- | --- |
| 1 | 🟢 提案 | カート画面（`CartView.tsx`）の数量セレクトは `MAX_QUANTITY_PER_ITEM=10` が選択肢の上限のため、UI 操作だけでは 1 明細に「11」を直接指定できない（`ST-F009-03` の 422 経路は商品詳細ページからの追加操作でのみ UI 上で再現できた）。テスト設計書にこの経路の指定を明記すると、次回以降の実施者が迷わない |
| 2 | 🟢 提案（解決済み・手法変更） | `ST-F014-06`（メール送信失敗）を OS のディレクトリ権限操作（`chmod`）で再現する手順は、本 Mac 環境では権限が数秒で自動復元されるため成立しなかった。代わりに「`.mail-outbox` を同名の通常ファイルに置換する」方式に変更したところ、`mkdir(parents=True, exist_ok=True)` が `FileExistsError` を送出し、権限に依存せず確実に再現できた（試行 2 で合格）。今後同種のテストをする場合はこの方式を先に使うとよい。恒久対策としては、`PAYMENT_STUB_RESULT=ok\|ng` と同様に環境変数でメール送信の成否を強制的に切り替えられるテスト用フック（例: `MAIL_STUB_RESULT=ng`）を `adapters/mail.py` に追加すると、ファイルシステム操作すら不要になり最も再現性が高い |
| 3 | 🟢 提案 | `ST-F025-02` と `ST-SEC-08` は「正しい注文番号＋違うメールで照会 → 404」という全く同じ操作・同じ期待結果になっており、実施記録も同一の証拠を指す。テスト設計書側で重複を明示するか、`ST-SEC-08` 側は「存在しない注文番号との応答形状の一致」まで踏み込んだ観点に寄せると、両者の狙いの違いがはっきりする |

### 3.5 良かった点

- 異常系・境界値のほぼ全項目（21/22）で期待結果どおりの挙動が確認でき、509 系エラー応答の固定文言化（`internal_error`／`db_unavailable`）、CSRF・内部トークン・CORS・IDOR の各防御、価格改ざん・在庫競合・決済 NG・価格変更時のロールバックが一貫して設計どおりに実装されていた。
- `ST-F014-02`（在庫 1 の競合）・`ST-F014-03`（価格変更）・`ST-F014-04`（決済 NG）はいずれも「注文は作られず、カートは active に戻り、在庫やカート状態が破損しない」という最も重要な性質を実データで確認できた。
- `system_settings`（税率・送料・閾値）の変更が API 再起動なしに即座に反映される設計（`ST-F032-01/02`）は、キャッシュを持たない実装のおかげで運用上の変更容易性が高い。
- セキュリティ系（`ST-SEC-02〜11`）は 8 件全て合格し、本番相当設定での Swagger 非表示、CORS のオリジン限定、金額・内部トークン・CSRF・DB 停止時のエラー応答隠蔽など、7 章の要求が実装に反映されていることを確認した。

### 3.6 環境を元に戻した証拠

- health（FastAPI 直）: `{"status":"ok","db":"ok"}`
- web（`http://localhost:3000`）: `200`
- `apps/api/.mail-outbox`: ST-F014-06 試行 2 で通常ファイルに置換した後、`rm` して退避先 `/private/tmp/guapp-mail-outbox-bak` から `mv` で復元。`ls -ld apps/api/.mail-outbox` → `drwxr-xr-x@ 8 mitsuru wheel 256 ... apps/api/.mail-outbox`（ディレクトリ・755 に復帰）。中身 `ls` → 6 件（`GU-260924-3XZP7DPF.txt`・`4LUTUF8M.txt`・`9WHAGSB8.txt`・`JT3CSYQR.txt`・`KQW3ETNX.txt`・`XY98ZQ84.txt`）で退避前と同数。試行 2 で確定した注文（`GU-260924-JR88VD4T`）はメール送信自体が失敗しているため、このファイルは存在しない（想定どおり）
- `system_settings`（SELECT 結果、seed 値と一致）:
  ```
  key                       value
  tax_rate                  "0.10"
  shipping_fee              550
  free_shipping_threshold   4990
  payment_timeout_minutes   30
  ```
- `products.id=12`（ST-F014-03 で変更した価格）: `price_incl_tax=1990`（seed 値に復元）
- API: `PAYMENT_STUB_RESULT`・`APP_ENV` とも上書き無しで再起動済み（`/docs` が `200` で開発モード復帰を確認）
- MySQL: `docker compose start mysql` 後、`guapp-mysql` は `Up ... (healthy)`
- `git status --short`: 本記録ファイル作成前と同一（`docs/00_計画/引継ぎメモ_20260924.md`・`docs/05_検収/ST-AT実施記録_正常系_20260918.md` の既存差分と `scripts/mysql-docker/` の未追跡のみ。コード・設定ファイルへの変更なし）
