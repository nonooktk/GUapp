# README 再現確認（P1 出口条件 4）— 2026-09-18

担当: けろっぴ（DevOps）／依頼: コーディネーター（Wave 3）
対象: `https://github.com/nonooktk/GUapp.git` HEAD `78f400b` を別フォルダ（`%TEMP%\claude\...\scratchpad\guapp-repro`）に新規 clone し、`README.md`「開発の始め方（P1）」を上から順に実行した。元リポジトリ・clone とも commit／push／checkout は行っていない。

## 結論

- **README の手順で環境は再現できた。失敗した手順は 0 件**。pre-commit → MySQL → API → web → テストまで通り、API health 200 ／ BFF `/api/cart` 200 を確認。
- ただし **README に書かれていない知識を 5 か所補った**（`.env` の分割・IT 用環境変数・ポート変更・ポート連動の値変更・`.env.local` の名前）。初見の人が詰まる可能性が高いのは「手順 3 で `DATABASE_URL` を埋める時点では credentials.txt がまだ無い」（手順順序）と「`uv run pytest -q` だけでは IT 76 件が skip される」の 2 点。
- 所要時間: **実行 5 分 33 秒**（16:00:05 clone 開始 → 16:05:38 サーバー停止確認。README 読解・記録作成を除く）。依存はすべてキャッシュ済み（uv・pnpm store）のため、初回 DL を含む環境では数分〜十数分増える見込み（推定）。

## 前提（本機の状態）

| 項目 | 状態 |
| --- | --- |
| Node / pnpm | v24.14.0 / 12.4.2（導入済み扱い） |
| uv / Python | uv 0.12.15。PATH の python は 3.12.10 だが `uv sync` が 3.13.15 を自動選択（README の「3.13（uv 経由）」どおり） |
| pre-commit | 4.6.2（導入済み扱い） |
| MySQL | `%LOCALAPPDATA%\guapp-mysql` に 8.4.11 を初期化済み・起動中（PID 7288, 20584） |
| 使用中ポート | 8000・3000・3306（別プロセス）。よって API は 8010、web は 3010 で起動 |
| DB | clone は元リポジトリと**同じ `guapp` DB** に接続する（credentials.txt 共有）。そのため alembic は「既に head」、seed は skip となった。空 DB からの投入経路は IT（`guapp_test` を reset して seed）で 207 件が通ることで代替確認 |

秘匿値（MySQL パスワード・`DATABASE_URL`）は本記録に書いていない。`.env` は clone 内に置き、`git status --ignored` で `!!`（ignore 済み）を確認。

## 実行した手順と結果

凡例: ✅ README のとおりに通った ／ ⚠️ 通ったが README に無い知識を補った ／ ❌ 通らなかった（今回 0 件）

| # | README の手順 | 実行したコマンド | 結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 必要ツール導入 | （導入済みとみなしスキップ。`pip install --user uv pre-commit detect-secrets` は未検証） | — | ※要確認（未実行） |
| 2 | `pre-commit install` | 同左 | `installed at .git\hooks\pre-commit` | ✅ |
| 2 | `pre-commit run --all-files` | 同左 | 10 フック すべて Passed（forbid .env files 含む）、10 秒 | ✅ |
| 3 | `.env.example` → `apps/api/.env`・`apps/web/.env` | api: `.env`（6 キー）／web: **`.env.local`**（5 キー）を作成。値は credentials.txt から機械的に転記 | 両ファイル ignore 済み（`!!`） | ⚠️ 名前・分割・ポート値を補った（穴 1・2・4・10） |
| 4 | `.\scripts\mysql-local\setup.ps1` | 同左 | exit 0。[1/6] zip スキップ・MD5 OK、[2/6] 展開スキップ、**[3/6] my.ini を再生成**、[4/6] data スキップ、[5/6]「ポート 3306 は既に開いています。起動済みとみなして何もしません」、**[6/6] credentials.txt を再保存**（ヘッダの生成時刻が 00:50:33 → 16:01:28 に更新。パスワードは不変＝以後の接続がすべて成功） | ✅（挙動は README 記述とややズレ。穴 6） |
| 4 | `.\scripts\mysql-local\status.ps1` | 同左 | 起動中 (PID 7288, 20584)・ポート開放・`SELECT VERSION()` = **8.4.11**・DB guapp, guapp_test | ✅ |
| 5 | `uv sync` | 同左 | CPython 3.13.15 で `.venv` 作成、44 packages、3 秒 | ✅ |
| 5 | `uv run alembic upgrade head` | **環境変数 `DATABASE_URL` を設定せず** `.env` のみで実行 | exit 0。`alembic current` = `6659ce6c5164 (head)`（既に head のため upgrade 出力なし） | ✅（api README の「$env:DATABASE_URL を設定」は不要だった。穴 5） |
| 5 | `uv run python -m app.seed`（1 回目） | 同左 | `[seed] DB 'guapp': 既に products に行があるため何もしませんでした（categories=16 / products=30 / variants=180 / product_images=30 / product_categories=30 / system_settings=4）。投入し直すには --reset を付けてください` exit 0 | ✅ |
| 5 | seed **2 回目** | 同左 | 1 回目と同一メッセージ・同一件数・exit 0（冪等を確認。行数増加なし） | ✅ |
| 5 | `uv run uvicorn app.main:app --port 8010` | `--reload` は付けず、ポートのみ 8010 | `Uvicorn running on http://127.0.0.1:8010`、startup complete | ⚠️ ポート変更は api README の `--port 8000` 表記から類推（穴 4） |
| 5 | `curl http://127.0.0.1:8010/api/v1/health` | 同左 | **200 `{"status":"ok","db":"ok"}`** | ✅ |
| 6 | `pnpm install` | `--frozen-lockfile` 付き | 436 packages（全て store から再利用）、12.6 秒 | ✅ |
| 6 | web README 3.「プレースホルダー生成（初回のみ）」 | 実行せず | `public/products/` に 30 ファイルが**既にコミット済み**で不要 | ✅（穴 7） |
| 6 | `pnpm dev` | **`pnpm dev -p 3010`** | `Next.js 16.3.5 (Turbopack)`・`Environments: .env.local`・Ready 653ms | ⚠️ `-p` は README に無い（穴 4） |
| 6 | `curl localhost:3010/` | 同左 | 200（`GET / 200 in 3.0s`）。本文に「読み込めませんでした」なし＝FastAPI 到達 | ✅ |
| 6 | `curl -i -c jar.txt localhost:3010/api/cart` | 同左 | 200。`set-cookie: cart_token=…; Path=/; Max-Age=7776000; HttpOnly; SameSite=Lax` と `csrf_token=…; SameSite=Lax`（HttpOnly なし）。本文 `item_count:0, free_shipping_threshold:4990, can_checkout:false` | ✅（web README「BFF の手動確認」と一致） |
| 6 | `curl localhost:3010/api/health` | 同左 | 200 `{"status":"ok","upstream":{"status":"ok","db":"ok"}}` | ✅ |
| 補 | `curl 127.0.0.1:8010/api/v1/products`（トークン無し） | 同左 | 401（内部認証が効いている） | ✅ |
| 6 | `uv run pytest -q`（README どおり・環境変数なし） | 同左 | **131 passed, 76 skipped** in 3.2s（IT 全件 skip） | ⚠️ IT を含めるには環境変数が必要（穴 3） |
| 6 | `uv run pytest -q`（`$env:DATABASE_URL_TEST` を設定） | 同左 | **207 passed** in 72.3s（unit 131 ＋ IT 76、skip 0） | ✅ |
| 補 | `uv run ruff check .` | 同左 | All checks passed! | ✅ |
| 6 | `pnpm test` | 同左 | **Test Files 16 passed / Tests 121 passed** in 5.25s | ✅ |
| 補 | `pnpm lint` / `pnpm typecheck` | 同左 | 両方 exit 0 | ✅ |
| 後片付け | サーバー停止 | `taskkill /PID <uv, cmd> /T /F` | プロセスツリー計 12 プロセス終了。16:05:38 時点で **8010・3010 とも Listen なし**。MySQL は元から起動していたので触らず | ✅ |

集計: README どおりに通った **19**、通らなかった **0**、補った **5**（.env の名前／.env の分割・値／`--port 8010`／`pnpm dev -p 3010` と連動する URL 値／`DATABASE_URL_TEST` の環境変数化）、未検証 **1**（手順 1 のツール導入コマンド）。

### 副産物（clone 内に生成されたもの・すべて gitignore 済み）

`apps/api/.venv`・`apps/api/.env`・`apps/api/.mail-outbox/`・`apps/web/node_modules`・`apps/web/.env.local`・`apps/web/.next`・`apps/web/AGENTS.md`・`apps/web/CLAUDE.md`（Next.js 16 が自動生成。`.gitignore` に登録済み）。`git status --short` は空。

## README の穴と修正案（文言レベル）

番号は上表の「穴 n」に対応。対象ファイルを明記する。

| # | 場所 | 事象（FACT） | 修正案 |
| --- | --- | --- | --- |
| 1 | ルート README 手順 3、`.env.example` 2 行目 | web 側を `apps/web/.env` と書くが、`apps/web/README.md` は `.env.local`。Next.js は両方読むので動くが表記が不一致 | 両方を **`apps/web/.env.local`** に統一（web README と一致）。`.env.example` のコメントも同様に修正 |
| 2 | ルート README 手順 3、`.env.example` | `.env.example` は api／web の両セクションを 1 ファイルに持つ。「コピーして値を埋める」とだけ書かれており、そのままコピーすると `APP_ENV`・`INTERNAL_TOKEN` が重複し、web 側に `DATABASE_URL=` の空行が入る（web の env ローダーが空値をどう扱うかは未検証 ※要確認） | 手順 3 を「**該当セクションの行だけ**を写す」と明記。または `.env.example` を `apps/api/.env.example`・`apps/web/.env.example` に分割する（分割が確実） |
| 3 | ルート README 手順 6、api README「テスト」 | `uv run pytest -q` だけでは IT 76 件が skip（131 passed / 76 skipped）。`tests/integration/conftest.py` は `os.environ` のみ読み、`.env` の `DATABASE_URL_TEST` は読まない。api README の例は `tests/integration` 限定で書かれ、全件実行の期待値が無い | 手順 6 に追記: 「IT を含めて全件（207 件）走らせるには `$env:DATABASE_URL_TEST = "<credentials.txt の DATABASE_URL_TEST>"` を**環境変数で**設定する（`.env` に書いても IT は読まない）。未設定なら 131 passed / 76 skipped が正常」 |
| 4 | ルート README 手順 5・6、web README 起動手順 4 | 8000／3000 が使用中の場合の手順が無い。api は `--port` が README に出ているので類推できるが、web の `pnpm dev -p <port>` は未記載。ポートを変えると api `.env` の `CORS_ALLOW_ORIGIN`、web `.env.local` の `API_BASE_URL`・`IMAGE_BASE_URL` も合わせる必要があることが書かれていない | 手順 5・6 の後に追記: 「ポート衝突時は `uv run uvicorn app.main:app --reload --port 8010`／`pnpm dev -p 3010`。その場合 api `.env` の `CORS_ALLOW_ORIGIN=http://localhost:3010`、web `.env.local` の `API_BASE_URL=http://127.0.0.1:8010`・`IMAGE_BASE_URL=http://localhost:3010` に揃える」 |
| 5 | api README「DB の初期化」 | `$env:DATABASE_URL = ...` を設定せよと書くが、`app/core/config.py` が `env_file=".env"` を読むため、手順 3 で `.env` を作っていれば環境変数なしで alembic・seed が通った（実測） | 「`apps/api/.env` に `DATABASE_URL` があれば環境変数の設定は不要。`.env` を使わない場合のみ `$env:DATABASE_URL` を設定する」に書き換え |
| 6 | `scripts/mysql-local/README.md`「setup.ps1 は冪等で…スキップする」 | 再実行時、[1/6][2/6][4/6] はスキップ表示だが **[3/6] my.ini と [6/6] credentials.txt は毎回再生成**される（credentials.txt のヘッダ時刻が更新）。パスワード自体は不変。[5/6] は「ポート 3306 は既に開いています。起動済みとみなして何もしません」と表示 | 「再実行すると my.ini と credentials.txt は書き直される（パスワード・data は変わらない）。mysqld が起動中なら [5/6] は『起動済みとみなして何もしません』と出る」を追記。また、ルート README 手順 4 のコメント「初回のみ」は「再実行しても安全」に変える |
| 7 | web README 起動手順 3 | 「プレースホルダー生成（初回のみ）」とあるが `public/products/001〜030/1.jpg` はコミット済みで、clone 直後に 30 ファイル存在。実行は不要 | 「リポジトリに含まれているため通常は不要。画像を作り直すときだけ `node scripts/gen-placeholders.mjs`」に変更 |
| 8 | `.env.example` api セクション | api README の環境変数表にある `DATABASE_URL_TEST`（IT 用）・`IMAGE_BASE_URL`・`TEST_RESERVE_DELAY_MS` が `.env.example` に無い（今回 `DATABASE_URL_TEST` を手で追加した） | `.env.example` の api セクションに `DATABASE_URL_TEST=`（コメント: IT 用。DB 名は `_test` 終わり）と `IMAGE_BASE_URL=` を追加。`TEST_RESERVE_DELAY_MS` はテスト専用なので README の表だけでよい |
| 9 | ルート README 手順 2 | `pre-commit run --all-files` は clone 直後に通る（問題なし）。ただし手順 1 の `pip install --user uv pre-commit detect-secrets` は本機で未検証 | 修正不要。初回 PC での検証を別途 1 回行うことを推奨（※要確認） |
| 10 | ルート README 手順 3 と 4 の**順序** | 手順 3 で `DATABASE_URL` を埋めるよう指示するが、その値（credentials.txt）は **手順 4 の setup.ps1 実行後にしか存在しない**。初見の人は手順 3 で止まる | 手順 3 と 4 を入れ替える。または手順 3 に「`DATABASE_URL` は手順 4 の後で credentials.txt から写す」と一文足す |
| 11 | ルート README 手順 5 | `--reload` 付きで書かれているが api README の起動例は `--reload --port 8000`。矛盾ではないが、両 README で「ポートを明示するかどうか」が揺れている | ルート README も `uv run uvicorn app.main:app --reload --port 8000` に揃える（穴 4 の追記と合わせる） |

## seed の 2 回目実行の挙動

- 1 回目・2 回目とも同一出力: `[seed] DB 'guapp': 既に products に行があるため何もしませんでした（categories=16 / products=30 / variants=180 / product_images=30 / product_categories=30 / system_settings=4）。投入し直すには --reset を付けてください`、exit 0。
- 件数が両回で一致し、`--reset` を付けない限り書き込みが起きないことを確認。api README「seed は冪等で、既に商品があれば何もしない」の記述どおり。
- 注: 本機では元リポジトリの作業で既に seed 済みだったため、「空 DB → 投入完了」のメッセージは今回の `guapp` では観測していない。空 DB 経路は IT（`guapp_test` に対する reset＋seed、`assert not result.skipped`）が 207 件中で通っていることで確認した。

## 検収判定（けろっぴ案）

P1 出口条件 4「README の手順だけで環境を再現できる」は **条件付き合格**。コマンド自体は全て通るが、穴 3（IT の環境変数）と穴 10（手順順序）は初見者が止まる確度が高いため、README 反映後に「合格」とするのを推奨。他の穴は表記統一・追記レベル。

## 参照

- 実行ログ（サーバー標準出力）: `%TEMP%\claude\C--Users-m-oya-Desktop-toolmaker\f4d9b742-3040-483e-b627-f0b1f2ca3b7e\scratchpad\repro-logs\`（uvicorn.*.log・pnpm-dev.*.log・pids.txt・jar.txt）
- clone: 同 `scratchpad\guapp-repro\`（`.env`・`.env.local` を含む。リポジトリへコピーしないこと）
