# GU EC サイト P1 ST／AT 実施記録（正常系）

| 項目 | 内容 |
| --- | --- |
| 実施日 | 2026-09-18（JST 15:59〜16:20） |
| 実施者 | ペックル（QA 楽観視点） |
| 対象 | テスト設計書 GUEC-TD-01 draft-v2: 2 章 AT-01〜08、3.1 区分「正常」全件、3.2 ST-NFR-01・02、ST-SEC-01・07・09 |
| 環境 | web `http://localhost:3000`（`pnpm dev`・Next.js 16.3.5）、FastAPI `http://127.0.0.1:8000`（`APP_ENV=development`、`PAYMENT_STUB_RESULT=ok`）、MySQL 8.4.11（seed 済み。開始時 orders 3 件） |
| ブラウザ | Claude Code 内蔵ブラウザ（Chromium）。幅は 1280×900／768×1024／375×812 をエミュレーション |
| 証拠の取り方 | 内蔵ブラウザの描画がタイムアウトし**スクリーンショットは取得不可**（タブが非表示で描画フレームが出ない）。代替として DOM のテキスト・`getComputedStyle`・`sessionStorage`・`performance.getEntriesByType("resource")` を JavaScript で読み取った値、curl のヘッダ、DB の SELECT 結果を証拠とする |
| 独立性 | ばつ丸（悲観 QA）とは相談・すり合わせをしていない |

ルール: 期待結果の文言をそのまま観測できたときだけ「合格」。観測できなかったもの・環境の都合で実施できないものは「未実施（理由）」。

記録した注文番号: **GU-260918-GX7EB5VV**（AT-01・1280px）、**GU-260918-9GDNWL7L**（AT-02・375px）

## 1. 受け入れテスト（AT）

| ID | 手順（実施したこと） | 期待結果 | 観測結果 | 判定 | 証拠 |
| --- | --- | --- | --- | --- | --- |
| AT-01 | 1280px。ホーム → WOMEN → ボトムス → 「イージーワイドパンツ」詳細でブラック／M／数量 2 → カートに入れる → カート → レジへ進む → ゲストで購入（既定選択）でお届け先・メール入力 → 確認画面へ → 注文を確定する → ダイアログ「確定する」 | 会員登録なしで完了画面に到達し注文番号 `GU-YYMMDD-XXXXXXXX` が表示。確認メールがログに出力。orders に受付済 1 件、order_items に確定時単価 | 完了画面 `/orders/complete?n=GU-260918-GX7EB5VV` に到達、注文番号 `GU-260918-GX7EB5VV`・注文日時 2026/09/18 16:04（日本時間）を表示。メールスタブ出力 `apps/api/.mail-outbox/GU-260918-GX7EB5VV.txt`（件名・宛先 test@example.com・明細・金額を含む）。DB: orders id=5 status=`accepted` subtotal 3980／shipping 550／total 4530／tax_rate_at_order "0.10"、order_items 1 行 variant 26 `GU-005-BK-M` unit_price_at_order 1990 × 2 product_name_at_order「イージーワイドパンツ」、payments stub/ok 4530、carts id=13 `ordered`、variants 26 stock 26 → 24 | 合格 | 確認画面テキスト: 小計 ¥3,980／送料 ¥550／合計 ¥4,530（税込）／うち消費税 ¥411。ゲスト radio `checked=true`（既定）、会員 radio `disabled` |
| AT-02 | 375px。ホーム → KIDS・TEEN → トップス → 「ボーダーロンT（キッズ）」詳細でネイビー／L → ページ末尾までスクロール → **下部固定バーの「カートに入れる」**で追加 → カート → レジへ進む → 確認画面へ → 注文を確定する → 確定する | 全操作が完了。スクロール中も下部固定バーからカート追加できる。横スクロール無し。固定バーの色・高さがデザイン基準 3 章準拠 | 完了画面 `/orders/complete?n=GU-260918-9GDNWL7L` に到達。固定バー: `position: fixed`、bottom=812=innerHeight（画面下端に密着）、高さ 77px、背景 白 95%（`bg-bg/95`）、上罫線 #E5E5E5、表示内容「ネイビー / L / 1 ／ ¥1,490 ／ カートに入れる」、ボタン高 52px・角丸 999px・font-weight 700。scrollY=1188=最大スクロール量の位置で追加操作 → バッジ 3→4、トースト「カートに追加しました（ネイビー / L × 1）」。6 画面すべて `scrollWidth`=375=innerWidth（横スクロール無し）。DB: orders id=6 `accepted` subtotal 7460／shipping 0／total 7460、order_items 4 行（1990・2990・990・1490 各 1）、payments stub/ok 7460、carts 15 `ordered`、variants 68: 29→28、74: 29→28 | 合格 | 注記: 固定バーのボタンの `getComputedStyle` が無効色（#D9D9D9/#8C8C8C）を返したが、`button.disabled=false`・class は `bg-primary`、`getAnimations()` で `CSSTransition` が `currentTime 0` で停止していた＝非表示タブで描画フレームが進まず 0.15s の色遷移が始値で止まった計測アーティファクト。実表示の目視確認はコーディネーター／ばつ丸に依頼（→ 総括 4） |
| AT-03 | `/products/29`（V-STOCK0 含む商品）でブラック／M（`GU-029-BK-M`）を選択。次にブラック／S、再度ブラック／M。`/products?gender=kids_teen` と ホームの一覧で P-ALL0（商品 28）を確認 | 「在庫切れ」が表示されカート追加ボタンが無効。P-ALL0 は一覧でも在庫切れ表示 | BK/M: `aria-live` 領域「在庫切れ」、ボタン文言「在庫切れ」`disabled=true`。BK/S: 「在庫あり」`disabled=false`。再度 BK/M で「在庫切れ」に戻る。一覧カード（商品 28）: 「在庫切れ ｜ 【P-ALL0】撥水パーカ（キッズ） ｜ 在庫切れ ｜ 2 色」（バッジと価格欄の両方が在庫切れ表示）。ホームのおすすめでも同様 | 合格 | DB: variants 163〜168（GU-028-*）stock 0、170 `GU-029-BK-M` stock 0（読み取りのみ） |
| AT-04 | 1280px カート。「イージーワイドパンツ」数量 2→3→2、既存明細「リブレギンス」を削除（確認ダイアログ→削除する）→ レジへ進む | 小計・送料・合計が即時再計算。ヘッダーのバッジが点数に追従。注文手続きへ遷移できる | 2→3: 小計 ¥6,960／送料 無料／合計 ¥6,960、バッジ 3→4。3→2: 小計 ¥4,970／送料 ¥550／合計 ¥5,520、バッジ 3。削除後: 小計 ¥3,980／送料 ¥550／合計 ¥4,530、バッジ 2、トースト「リブレギンス（キッズ）【V-STOCK0あり】 を削除しました」。「レジへ進む」→ `/checkout`（タイトル「ご注文手続き」） | 合格 | 各操作後 2 秒以内に DOM が更新（wait 2s 後の読み取りで反映済み） |
| AT-05 | 確認画面で「注文を確定する」→ ダイアログ → 「確定する」。MutationObserver でボタン状態を記録 | 確認ダイアログが出る。確定中はボタン無効・処理中表示。完了後にトースト | `role=dialog`「ご注文を確定しますか？／この内容で注文を確定します。よろしいですか？／キャンセル／確定する」。状態遷移の記録: `確定する:E` → `処理中…:D aria-busy=true`（「注文を確定する」ボタンも同時に `処理中…:D`、キャンセルも `D`）→ ダイアログ消去 → 完了画面でトースト「ご注文ありがとうございます」 | 合格 | AT-01・AT-02 の両方で同じ遷移を観測 |
| AT-06 | AT-01 完了画面の「買い物を続ける」→ ホーム（バッジ 0）→ `/products/12` ドライポロシャツ ネイビー／M を追加 | 新しい空のカートに追加できる（注文済みカートに混ざらない） | 追加後 `GET /api/cart`: item_count 1、items=[item_id 16, variant 68]。DB: cart_items 16 は carts 15（`active`）、注文済み carts 13（`ordered`）の cart_items は 15（variant 26 × 2）のまま | 合格 | バッジ 0→1 |
| AT-07 | ホームの WOMEN／MEN／KIDS・TEEN リンク、一覧ページの性別タブ・子カテゴリリンク（`nav[aria-label=カテゴリ]`）で遷移。ヘッダーの「≡ メニュー」（`aria-label="メニューを開く"`）をクリック。各ページでヘッダー導線を確認 | 各区分・各カテゴリの一覧に到達し件数が表示。ヘッダーの検索・お気に入り・カート・マイページ導線が全ページにある | WOMEN 10 件／MEN 10 件／KIDS・TEEN 9 件（非公開 1 点を除外）、WOMEN>ボトムス 3 件、KIDS・TEEN>アウター 3 件、KIDS・TEEN>トップス 3 件を表示。ヘッダー導線（GU ホーム・検索・お気に入り・カート（N点）・会員）は 7 画面すべてで存在。**一方、ヘッダーの「≡ メニュー」ボタンはクリックしても何も開かない**（`aria-expanded` が常に `false`、`role=dialog` 出現なし。`components/Header.tsx` 64〜76 行にハンドラ無し、コメント「中身は Wave 1 以降で実装」）。ヘッダー自体に WOMEN／MEN／KIDS・TEEN タブは無く、性別区分への導線はホーム本文と一覧ページの「カテゴリ」ナビのみ | **不合格（部分）** 🟡 | 期待結果の「オールメニューのカテゴリから商品一覧へ遷移」が実施不能。性別・カテゴリ一覧への到達と件数表示、ヘッダー 4 導線は合格。再現手順は総括 2 |
| AT-08 | 1280px。カートを ドライポロシャツ 1,990 × 1 ＋ オックスフォードシャツ 2,990 × 1 ＝ 小計 4,980 にして確認画面。次に ドライボクサーブリーフ 990 を追加して小計 5,970 で確認画面 | 4,989 円は送料 550、4,990 円は送料 0。「うち消費税」＝合計×10÷110 の円未満切り捨て | **小計 4,989・4,990 は seed の価格では組めない**（seed の全価格 990/1,490/1,990/2,490/2,990/3,990 はすべて下 2 桁 90 のため、n 点の小計 ≡ 90n (mod 100)。4,990 には n≡1 (mod 10) が必要だが 4,990 円の単品は無い。4,989 は奇数で不可）。閾値未満で最も近い 4,980 と、閾値以上で組める 5,970 で確認。4,980: 送料 ¥550／合計 ¥5,530／うち消費税 ¥502（5530×10÷110=502.7→502）。5,970: 送料 無料／合計 ¥5,970／うち消費税 ¥542（542.7→542）。AT-01 の 4,530 → ¥411（411.8→411）、AT-02 の 7,460 → ¥678（678.1→678） | 合格（境界の片側のみ。閾値 4,990 ちょうどと 4,989 は未確認） | テスト設計書 6 章「単価 1,990・2,990・1,490 の商品で 4,989／4,990 を組み立て」は seed では成立しない → 総括 3 に記載 |

## 2. システムテスト 3.1 機能要件（区分「正常」）

| ID | 手順 | 期待結果 | 観測結果 | 判定 | 証拠 |
| --- | --- | --- | --- | --- | --- |
| ST-F001-01 | ホーム → WOMEN → ボトムス | 該当カテゴリの公開商品のみが新着順で表示され総件数が出る | `/products?gender=women&category=women-bottoms`: 「WOMEN > ボトムス ／ 3 件」、カードは `/products/6`,`/5`,`/4` の順（id 降順＝新着順）。全件一覧 29 件（公開 29／非公開 1 除外）で先頭 29,28,27 | 合格 | パンくず「ホーム > WOMEN > ボトムス」も表示 |
| ST-F001-03 | 25 点以上のカテゴリが seed に無いため、全件一覧 `/products`（29 件）で「もっと見る」 | 24 件で区切られ次の 24 件が追加表示 | 初期表示 24 カード・「24 / 29 件を表示」・「もっと見る」ボタンあり → クリック後 29 カード・「29 / 29 件を表示」・ボタン消滅。末尾は `/products/3`,`/2`,`/1` | 合格（注記: 単一カテゴリ 25 点以上は seed に無く、全件表示で代替） | 1280px・768px の両方で 24 件区切りを観測 |
| ST-F004-01 | 6 画面（ホーム／一覧／詳細／カート／注文手続き／確認）＋完了でヘッダー・フッターを確認 | ヘッダー（メニュー・ロゴ・検索・お気に入り・カート点数・会員）とフッター（企業情報・利用規約・プライバシー・特商法・FAQ）が全画面に出る | 全 7 画面で `header` 内に「メニューを開く／GU ホーム／検索／お気に入り／カート（N点）／会員」、`footer` にリンク 5 件（企業情報・利用規約・プライバシー・特商法・FAQ）。3 幅すべてで同じ | 合格 | 注記: 「メニュー」は要素として存在するが開かない（AT-07 参照）。検索・お気に入り・会員・フッターの `href` は `#`（P1 は導線のみ） |
| ST-F005-01 | `/products/5` を開く | 画像・名称・税込価格・説明・素材・サイズ表・色/サイズ選択・数量・関連商品 | 画像 `http://localhost:3000/products/005/1.jpg`、名称「イージーワイドパンツ」、「¥1,990（税込）」、色（ブラック／ベージュ）、サイズ（S/M/L）、数量セレクト 1〜10、商品説明、素材「ポリエステル100%」、サイズ表（S/M/L）、関連商品 2 点（フレアロングスカート ¥2,490・ハイウエストストレートジーンズ ¥2,990） | 合格 | 未選択時は「色とサイズを選択してください」 |
| ST-F005-02 | `/products/29` で色・サイズを切り替え | 選んだ組み合わせの在庫有無が表示に反映 | BK/M「在庫切れ」→ BK/S「在庫あり」→ BK/M「在庫切れ」（`aria-live` とボタンの両方が追従） | 合格 | AT-03 と同じ観測 |
| ST-F005-03 | 375px `/products/22` で末尾までスクロール | 下部固定バーに選択内容と価格・カート追加ボタンが常に表示 | scrollY 1188（最大）でバー `display:block`・`fixed`・bottom=812。内容「ネイビー / L / 1 ／ ¥1,490 ／ カートに入れる」。フッターが視野内でもバーが表示 | 合格 | 1280px・768px ではバー `display:none`（デスクトップでは非表示の設計） |
| ST-F009-01 | 追加（AT-01/06）・数量変更（AT-04）・削除（AT-04）。同じバリエーション（variant 68）を 2 回追加 | 一覧に名称・色・サイズ・単価・数量・小計・合計。同じバリエーションの再追加は数量加算 | カート行「ドライポロシャツ ／ ネイビー / M ／ 単価 ¥1,990 ／ 数量 ／ 削除 ／ 小計」、金額欄に小計・送料・合計。2 回追加後 `GET /api/cart`: items 1 行（item_id 16）quantity 2、subtotal 3980 | 合格 | バッジ 1→2 |
| ST-F009-02 | 内蔵ブラウザは「閉じて再訪」ができないため、(a) 同一 Cookie で別ページ遷移・再読み込みを繰り返してカートが保持されること、(b) curl で `GET /api/cart` の Set-Cookie 属性と Cookie 再送時の挙動を確認 | Cookie の匿名トークンでカートが保持（90 日） | (a) `/products/*` → `/cart` → `/checkout` → ホームの往復でバッジと明細が一貫。(b) `Set-Cookie: cart_token=…; Path=/; Max-Age=7776000; HttpOnly; SameSite=Lax`（7,776,000 秒＝90 日）。同じ jar で再要求 → 同じカートが返り Set-Cookie は付かない（0 件） | 合格（ブラウザ終了→再起動の操作は環境上未実施。Cookie 属性と再送で代替） | `wave3-qa/jar.txt` |
| ST-F013-01 | 注文手続きで配送先・メールを入力し確認画面へ | 明細・金額（小計・送料・合計・うち消費税）・配送先・メール・受け取り方法（配送）・支払い方法（カード） | 確認画面: 明細（名称・色/サイズ・単価×数量・小計）、小計 ¥3,980／送料（4,990 円以上で無料）¥550／合計 ¥4,530（税込）／うち消費税 ¥411、お届け先「テスト 太郎 様／〒100-0001／東京都　千代田区　千代田1-1　テストビル 101／電話 09012345678」、メール test@example.com、受け取り方法「配送」、支払い方法「クレジットカード（テスト決済）」 | 合格 | `sessionStorage.guapp.checkout` に prepare 応答（idempotency_key・items・金額）が保存 |
| ST-F014-01 | AT-01 注文確定後に DB を SELECT | 注文番号・明細・金額が表示。orders 受付済、order_items に確定時単価と商品名、payments に stub/ok、audit_logs に記録、variants.stock が数量分減る | 画面: 注文番号・明細・金額を表示。orders id=5 `accepted`、order_items unit_price_at_order 1990・product_name_at_order「イージーワイドパンツ」、payments provider `stub` result `ok` amount 4530（カード情報の列なし・provider_ref 空）、audit_logs id=4 `order.confirm`、variants 26 stock 26→24（数量 2） | 合格 | AT-02（GU-260918-9GDNWL7L）でも同じ 4 点セットを確認 |
| ST-F014-05 | 2 件の注文番号を `GU-\d{6}-[A-HJ-NP-Z2-9]{8}` と照合 | `GU-`＋日付 6 桁＋`-`＋英大文字数字 8 文字。I・O・0・1 を含まない | `GU-260918-GX7EB5VV`、`GU-260918-9GDNWL7L` とも一致。日付 260918＝JST 当日。I/O/0/1 なし | 合格 | Python `re.fullmatch` で True |
| ST-F025-01 | 別タブ（sessionStorage 無し）で `/orders/complete?n=GU-260918-GX7EB5VV` → 照会フォームに注文番号と test@example.com を入力 → 注文を表示する | 注文内容が表示 | フォーム「ご注文の確認／注文番号（必須）／メールアドレス（必須）」が出る（注文番号は URL から自動入力済み）。送信で `POST /api/orders/GU-260918-GX7EB5VV/lookup` が 1 回呼ばれ、注文番号・注文日時・明細・お届け先・金額（¥4,530／うち消費税 ¥411）が表示 | 合格 | 通信は `/api/*` のみ（8000 番ポートへの直接通信 0 件） |
| ST-F032-01 | — | tax_rate 変更で「うち消費税」が再計算 | — | 未実施（system_settings の変更に DB 直接 UPDATE が必要で、本タスクは DB 読み取りのみ。ばつ丸に委譲） | 現在値 tax_rate "0.10"／shipping_fee 550／free_shipping_threshold 4990（`GET /api/v1/settings/public` と DB で一致） |
| ST-F032-02 | — | shipping_fee・free_shipping_threshold 変更が再デプロイ不要で反映 | — | 未実施（同上） | — |
| ST-F036-01 | 注文確定（AT-05）と削除（AT-04）の操作 | 実行前に確認ダイアログ。処理中はボタン無効と処理中表示。成功・失敗をトーストで表示 | 削除: `role=dialog`「商品を削除しますか？／…をカートから削除します。／キャンセル／削除する」→ 成功トースト。確定: ダイアログ → `処理中…` `disabled` `aria-busy=true` → 成功トースト「ご注文ありがとうございます」 | 合格（成功側のみ。失敗トーストは異常系のためばつ丸範囲） | AT-04・AT-05 の観測 |

## 3. システムテスト 3.2 非機能・セキュリティ

| ID | 手順 | 期待結果 | 観測結果 | 判定 | 証拠 |
| --- | --- | --- | --- | --- | --- |
| ST-NFR-01 | 375／768／1280 の 3 幅で 6 画面（ホーム・一覧・詳細・カート・注文手続き・確認）を表示し `scrollWidth`・グリッド列数（`grid-template-columns` の要素数）・ヘッダー/フッターを読む | レイアウト崩れ・横スクロール無し。商品グリッド 1280: 4 列／768: 3 列／375: 2 列。色・書体・ボタン形状はデザイン基準 2〜3 章 | 横スクロール: 375 は全画面 `scrollWidth`=375=innerWidth、768 は 753（=768−スクロールバー 15）、1280 は 1265（同）→ いずれも内容幅が viewport を超えない。商品一覧グリッド: 1280→4 列、768→3 列、375→2 列。ホームのおすすめ: 1280→4、768→4、375→2。ホームのカテゴリ: 3 列（375 でも 3 列＝基準 4 章の補正どおり）。ボタン: 主 CTA 高 52px・`rounded-pill`(999px)・font-weight 700・背景 #1B1B1B（rgb(27,27,27)）／文字 #FFFFFF。完了画面の注文番号 24px（`--text-xl`）・700 | 合格（数値で確認できる項目のみ。「崩れ」の目視は未実施） | ホームのおすすめグリッドが 768px で 4 列になる点は基準 4 章に 768 の規定が無く判定対象外として記録（総括 4） |
| ST-NFR-02 | AT-01 注文の audit_logs を SELECT | actor_type=system、action=order.confirm、target=注文番号、at が記録 | `actor_type=system ／ action=order.confirm ／ target_type=order ／ target_id=GU-260918-GX7EB5VV ／ detail={"total": 4530, "provider": "stub"} ／ at=2026-09-18 07:04:16`（UTC） | 合格 | AT-02 の注文も同様に 1 行（at 07:16:34） |
| ST-SEC-01 | 詳細→カート追加→カート、完了画面、照会の各ページで `performance.getEntriesByType("resource")` の URL を確認 | FastAPI のホスト・ポートへの直接通信が 1 件もない。`/api/*` のみ | 8000 番を含む URL 0 件（リソース 69 件中）。API 呼び出しは `http://localhost:3000/api/cart`・`/api/cart/items`・`/api/orders/GU-…/lookup` のみ | 合格 | 照会タブでも 8000 番 0 件 |
| ST-SEC-07 | ブラウザで `document.cookie`、curl で Set-Cookie 属性 | HttpOnly・SameSite=Lax。`document.cookie` で読めない | `document.cookie` の名前は `csrf_token` のみ（`cart_token` は読めない）。`Set-Cookie: cart_token=…; Path=/; Max-Age=7776000; HttpOnly; SameSite=Lax`。`csrf_token` は HttpOnly なし・SameSite=Lax（Double Submit 用の設計どおり） | 合格 | `Secure` は `APP_ENV=production` のみ付与の設計（development のため未付与、想定どおり） |
| ST-SEC-09 | `apps/web/.next/static`（144 ファイル。`.next/BUILD_ID` ありのビルド済み成果物）を `dev-local-token`・`INTERNAL_TOKEN`・`DATABASE_URL`・`mysql+asyncmy` で全文検索 | `INTERNAL_TOKEN`・`DATABASE_URL` の値が 0 件 | `.next/static` 内: `dev-local-token` 0 件、`INTERNAL_TOKEN`／`DATABASE_URL`／`mysql+asyncmy` 0 件 | 合格 | `pnpm build` は dev サーバー稼働中のため再実行せず既存ビルドを検査。CI でも検査済みとの申し送り |

## 4. 総括

### 4.1 件数

| 判定 | 件数 | ID |
| --- | --- | --- |
| 合格 | 25 | AT-01, 02, 03, 04, 05, 06, 08（片側）, ST-F001-01, 001-03, 004-01, 005-01, 005-02, 005-03, 009-01, 009-02, 013-01, 014-01, 014-05, 025-01, 036-01, NFR-01, NFR-02, SEC-01, SEC-07, SEC-09 |
| 不合格 | 1 | AT-07（部分: オールメニューが開かない） |
| 未実施 | 2 | ST-F032-01, ST-F032-02（DB 変更禁止のためばつ丸に委譲） |

合計 28 件（AT 8、ST 3.1 正常 15（ST-F001-03 を含む）、ST 3.2 5）。

P1 出口条件（AT-01 合格、375px で導線が通る）は満たした。

### 4.2 不合格の一覧（再現手順つき）

| # | 重大度 | ID | 観測結果 | 再現手順 | 該当 DS |
| --- | --- | --- | --- | --- | --- |
| 1 | 🟡 要修正 | AT-07 | ヘッダーの「≡ メニュー」（オールメニュー）を押しても何も起きない。`aria-haspopup="dialog"` `aria-expanded={false}` が固定値で、開閉ハンドラが無い | 1. 任意の幅で `http://localhost:3000/` を開く 2. ヘッダー左の「≡ メニュー」をクリック 3. 何も表示されない（`document.querySelectorAll('[role=dialog]').length` が 0、`aria-expanded` が `false` のまま）。ソース: `apps/web/components/Header.tsx` 64〜76 行（コメント「中身は Wave 1 以降で実装」） | DS-SCR 共通（U-06）・要件 F-004／REQ-FR-109。設計仕様書 580 行「P1 では性別カテゴリと商品カテゴリの階層だけを実装する」に対し未実装。性別・カテゴリへの導線はホーム本文と一覧ページのナビで代替可能なため 🔴 ではなく 🟡 |

### 4.3 テストデータ・テスト設計への気づき（バグではない）

| # | 重大度 | 内容 |
| --- | --- | --- |
| 1 | 🟡 要修正（テスト設計書／seed） | AT-08 の「小計 4,989／4,990」が seed の価格では組み立て不能（全価格が下 2 桁 90 のため、n 点の合計は 90n mod 100。4,990 には単品 4,990 円か 11 点が要る）。`seed_data.REQUIRED_PRICES = {1990, 2990, 1490}` は「閾値の組み立てに必ず要る価格」とコメントされているが、この 3 価格では 4,980 が上限で 4,990 に届かない。閾値ちょうどの境界は UT-021-02（関数単位）でのみ担保されている。対策案: seed に 4,990 円の商品（または 2,495 円 × 2 のような組み合わせ）を追加するか、AT-08 の期待値を「4,980 → 550 円／閾値以上の最小組み合わせ → 0 円」に書き換える |
| 2 | 🟢 提案 | ST-F001-03 は seed（1 カテゴリ最大 4 点）では「25 点以上のカテゴリ」が存在せず、全件一覧（29 件）で代替した。設計書の手順を「全件一覧」に改めるか、seed を 25 点以上のカテゴリを含む構成にする |

### 4.4 改善提案（使ってみて気づいた点。3 件まで）

| # | 重大度 | 内容 |
| --- | --- | --- |
| 1 | 🟢 提案 | 確認メール本文の「注文日時」が `2026-09-18 07:04:16+00:00`（UTC）で出力される。完了画面は「2026/09/18 16:04（日本時間）」と JST 表記なので、メールも JST に揃えると利用者が迷わない（`apps/api/app/adapters/mail.py` `render_order_confirmation` の `order.ordered_at`） |
| 2 | 🟢 提案 | ホームのおすすめ商品グリッドが 768px で 4 列（一覧は 3 列）。デザイン基準 4 章 SCR-001 は 1280: 4 列／375: 2 列のみ規定しており違反ではないが、一覧と列数を揃える（768: 3 列）と統一感が出る |
| 3 | 🟢 提案 | 固定購入バー（375px）のボタンは色遷移 `transition-colors` 0.15s 付き。今回の環境では描画停止のため始値（無効色）で止まって見えた。実機では問題ないはずだが、無効→有効の切り替わりを目視で 1 回確認しておくと安心（本記録では「実表示未確認」） |

### 4.5 良かった点

- ゲスト購入が既定で選ばれており、会員導線（P2）は `disabled`＋「P2 対応予定」で迷わない。
- 注文手続きフォームは 2 回目以降 `sessionStorage` から前回入力値を復元し、再入力なしで確認画面へ進める。照会フォームも URL の `?n=` から注文番号を自動入力する。
- 確定ボタン押下から完了画面までの状態遷移（ダイアログ → 処理中… → トースト）が一貫し、二重送信の余地がない（3 ボタンすべて `disabled`）。
- 金額表示は全画面で `¥1,990` 形式、内税は 4 パターン（¥411／¥502／¥542／¥678）すべて円未満切り捨てと一致。
- BFF 経由の通信のみで、クライアントから FastAPI・内部トークンが一切見えない（ST-SEC-01/07/09）。

### 4.6 未実施の理由

| ID | 理由 |
| --- | --- |
| ST-F032-01 | `system_settings.tax_rate` の変更に DB 直接 UPDATE が必要。本タスクは DB 読み取りのみのためばつ丸（悲観 QA）に委譲 |
| ST-F032-02 | 同上（`shipping_fee`・`free_shipping_threshold`） |

部分的に代替した項目: AT-08（4,989／4,990 ちょうどは seed で不可 → 4,980／5,970）、ST-F001-03（25 点以上のカテゴリ無し → 全件 29 件）、ST-F009-02（ブラウザ終了→再起動は不可 → Cookie 属性と curl 再送）、ST-NFR-01（目視の「崩れ」判定は不可 → `scrollWidth`・列数・計算スタイル）、全項目のスクリーンショット（内蔵ブラウザの描画タイムアウトで取得不可 → DOM 観測値）。

### 4.7 テストデータの前提

V-STOCK1（`GU-027-NV-M`、在庫 1）は購入していない（在庫 1 のまま）。P-UNPUB（商品 30）・P-ALL0（商品 28）の状態は変更していない。DB への書き込みはアプリ経由の注文 2 件（orders 3 → 5 件）と、それに伴う在庫減少（variants 26: −2、68: −1、74: −1、116: −1、129: −1）のみ。
